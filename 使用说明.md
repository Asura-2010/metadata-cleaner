# 元数据清除工具 — 使用说明

## 功能

一键清除 Office（Word/Excel/PPT）、WPS 和 PDF 文件中隐藏的个人/公司信息。

**清除的元数据：**

| 清除内容 | 示例 |
|----------|------|
| 作者 / 修改者 | 张三、李四 |
| 公司名 / 管理者 | XX有限公司、王经理 |
| 创建时间 / 修改时间 | 2024-01-15 14:30 |
| 版本号 / 修订号 | 3.1、42 |
| 标题 / 主题 / 描述 | 机密报告、财务数据 |
| 自定义属性 | 第三方插件写入的路径、标识等 |

---

## 批注和修订记录处理

工具**能自动检测**文档中的批注和修订记录，但**无法自动清除**（它们嵌在正文 XML 深处）。

检测到批注/修订时，会弹出提示窗口，指导你去 Office 或 WPS 中手动清理：

1. **审阅 → 接受所有修订**（清除修订记录中的作者名）
2. **审阅 → 删除文档中的所有批注**
3. 保存后重新拖入本工具处理

---

## 安装

### Windows

1. 双击 `setup.bat`
2. 如果提示未安装 Python，点击"是"打开下载页面（默认用清华镜像，国内速度快）
3. 安装 Python 时 **务必勾选**「Add Python to PATH」
4. Python 装完后重新双击 `setup.bat` 完成依赖安装

### macOS

1. 打开终端（启动台搜索"终端"）
2. 把 `setup.sh` 拖到终端窗口，按回车
3. 如果提示未安装 Python，点击对话框中的"打开清华镜像"下载安装

---

## 使用方法

### 方法一：图形界面（推荐）

双击 `metadata_cleaner.py`，弹出窗口后：

1. 点击 **「添加文件」**，选择要处理的文件（可多选）
2. 点击 **「清除元数据」**
3. 如有批注/修订，会先弹出警告，按提示去 Office 清理后再回来
4. 确认后自动处理，完成后弹窗提示

### 方法二：拖拽批量处理

把文件直接拖到 `metadata_cleaner.py` 图标上，自动处理并显示结果。

---

## 支持的文件格式

| 类型 | 扩展名 |
|------|--------|
| Word 文档 | .docx |
| Excel 表格 | .xlsx |
| PowerPoint 演示 | .pptx |
| WPS 文字 | .wps |
| WPS 表格 | .et |
| WPS 演示 | .dps |
| PDF 文件 | .pdf |

> 旧版格式（.doc / .xls / .ppt / 旧 .wps）不支持。

---

## 安全设计

| 机制 | 说明 |
|------|------|
| 原子替换 | 先写临时文件，成功后再替换；中途断电不会损坏原文件 |
| 流式处理 | 逐条读写 ZIP 条目，500MB+ 文件也不会耗尽内存 |
| defusedxml | 使用安全 XML 解析库，防御 XXE 和 XML 炸弹攻击 |
| 分类报错 | 文件占用、格式不支持等都有中文提示，不会显示原始错误码 |

---

## 常见问题

**Q: 处理后文件会损坏吗？**
不会。采用原子替换机制，即使写入中途断电，原文件也完好无损。

**Q: 能恢复清除的元数据吗？**
不能。清除是永久性的。建议处理敏感文件前先备份。

**Q: 提示"文件正被其他程序占用"？**
先关闭 Word / Excel / WPS 中打开的该文件，再重试。

**Q: 提示"不是有效的 Office/WPS 文档"？**
说明文件是旧版二进制格式，本工具不支持。请在 Office 中另存为 .docx 格式。

**Q: 旧格式 .doc / .xls / .ppt 能转换后处理吗？**
能。先用 Office 打开，"另存为" .docx / .xlsx / .pptx，再处理。

**Q: pip 安装太慢？**
setup 脚本已配置清华镜像，下载速度应该正常。如仍有问题，可手动切换镜像：
```
pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/
```
